// square of a number

#include <stdio.h>
int main()
{
    int a, mul;
    printf("Enter a number: ");
    scanf("%d", &a);
    mul = a * a;
    printf("The square of a is %d", mul);
    return 0;
}
